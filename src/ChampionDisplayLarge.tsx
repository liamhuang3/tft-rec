import React from 'react';
import { Box, Paper } from '@mui/material';

interface ChampionDisplayLargeProps {
    iconUrls: string[]; // Array of image URLs
    sideLength: number;
  }

  const ChampionDisplayLarge: React.FC<ChampionDisplayLargeProps> = ({ iconUrls, sideLength }) => {
    return (
        <div>

        </div>
    );
  }

  export default ChampionDisplayLarge;